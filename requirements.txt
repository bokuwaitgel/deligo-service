fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
googlemaps>=4.10.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.0
pytest>=8.0.0
httpx>=0.27.0
# Cross-worker fan-out for the SSE notification stream. Optional at runtime:
# without REDIS_URL the event bus degrades to single-process delivery.
redis>=5.0.0
# Web Push (RFC 8030 + VAPID) so notifications reach customers whose tracking
# tab is closed. Optional at runtime: without VAPID_PUBLIC_KEY /
# VAPID_PRIVATE_KEY the push service is a no-op and SSE alone is used.
# Pulls in py-vapid and cryptography (also used by scripts/generate_vapid_keys.py).
pywebpush>=2.0.0
